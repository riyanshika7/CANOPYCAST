fastapi==0.121.2
uvicorn[standard]==0.39.0
pydantic==2.12.3
python-dotenv==1.1.1

# storage
chromadb==1.4.1

# retrieval: dense (chroma) fused with lexical (bm25). Species names are exact
# tokens that dense embeddings blur together, so lexical is not optional here.
rank-bm25==0.2.2

# corpus ingest
pypdf==6.1.3
langchain-text-splitters==0.3.9

# gpt-5.6-luna for generation, text-embedding-3-small for embeddings
openai==2.9.0

httpx==0.28.1
pytest==8.3.5
